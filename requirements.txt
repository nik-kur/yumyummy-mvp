fastapi
uvicorn[standard]
SQLAlchemy
alembic
psycopg2-binary
pydantic>=2.12.3
aiogram>=3.22.0
httpx
python-dotenv
redis
pydantic-settings>=2.3.0
# openai 2.45.0 made `cache_write_tokens` a required field on
# InputTokensDetails, which openai-agents==0.4.0 does not pass when it
# builds Usage() -> every Runner.run() crashes at entry. Cap below 2.45
# until openai-agents is upgraded to a release with the fix (>=0.18.x).
openai>=2.9.0,<2.45
openai-agents==0.4.0
pyyaml>=6.0
python-multipart
anyio
pytz
sentry-sdk[fastapi]>=2.18.0
posthog>=3.7.0
# Mobile app: JWT auth (our access tokens + verifying Apple/Google identity
# tokens via JWKS). The [crypto] extra pulls in `cryptography` for RS256.
PyJWT[crypto]>=2.8.0
# Mobile app: object storage (Cloudflare R2 / AWS S3) for meal photos
boto3>=1.34.0
